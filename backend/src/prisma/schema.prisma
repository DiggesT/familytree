generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Idea {
  id         String @id @default(uuid())
  lastName   String
  firstName  String
  middleName String
  mother     String
  father     String
}
